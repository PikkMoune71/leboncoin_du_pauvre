<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">Leboncoin du Pauvre</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{ path('app_homepage') }}">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{ path('articles') }}">Annonces</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{ path('admin') }}">Admin</a>
                </li>
            </ul>

            {% if app.user %}
                <li class="nav-item m-2 list-unstyled">
                    <a class="btn btn-warning" href="{{ path('account') }}">Account</a>
                </li>
                <li class="nav-item m-2 list-unstyled">
                    <a class="btn btn-danger" href="{{ path('app_logout') }}">Logout</a>
                </li>
            {% else %}
                <li class="nav-item m-2 list-unstyled">
                    <a class="btn btn-warning" href="{{ path('register') }}">Sign-up</a>
                </li>
                <li class="nav-item m-2 list-unstyled">
                    <a class="btn btn-success" href="{{ path('app_login') }}">Sign-in</a>
                </li>
            {% endif %}


        </div>
    </div>
</nav>
